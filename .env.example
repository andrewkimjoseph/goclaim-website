# GoClaim subgraph (public GraphQL endpoint)
VITE_GOCLAIM_SUBGRAPH_URL=

# Optional: override quote API for local dev (defaults to https://app.goclaim.xyz/api/g-usdm-quote)
# VITE_GD_QUOTE_API_URL=http://localhost:3000/api/g-usdm-quote

# Mirror app enrollment pause on the marketing site (set false when paused).
# VITE_GOCLAIM_ACCOUNT_CREATION_ENABLED=true
